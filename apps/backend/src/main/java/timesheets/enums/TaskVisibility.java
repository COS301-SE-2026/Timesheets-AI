package timesheets.enums;

//This enum represents the visibility of a task in the timesheet application.
//PUBLIC: The task is visible to all members of the workspace.  
//PRIVATE: The task is only visible to members who have been granted access to it.
public enum TaskVisibility {
    PUBLIC,
    PRIVATE
}
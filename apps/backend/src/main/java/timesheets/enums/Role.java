package timesheets.enums;

//This enum represents the different roles that a user can have in the system.
//It is used to determine what actions a user is allowed to perform based on their role.
public enum Role {
   DEVELOPER,
   MANAGER,
   ADMIN
}
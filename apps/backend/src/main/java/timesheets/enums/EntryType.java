package timesheets.enums;

//This enum represents the type of time entry in the timesheet application,
//which can be either a manual entry where the user manually inputs the time,
//or a timer entry where the time is automatically tracked.
public enum EntryType {
    MANUAL,
    TIMER
}
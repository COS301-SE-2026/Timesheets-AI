package timesheets.enums;

//This enum represents different types of entities in the timesheet application,
//which can be used for various purposes such as logging, notifications, or access control.
public enum EntityType {
    USER,
    WORKSPACE,
    PROJECT,
    TASK,
    TIME_ENTRY,
    TIMER_SESSION,
    WORKSPACE_MEMBER,
    WORKSPACE_ROLE
}
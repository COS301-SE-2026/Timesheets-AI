package timesheets.enums;

//This enum represents the visibility of a project in the timesheet application.
//PUBLIC: The project is visible to all members of the workspace.
//PRIVATE: The project is only visible to members who have been granted access to it.

public enum ProjectVisibility {
    PUBLIC,
    PRIVATE
}
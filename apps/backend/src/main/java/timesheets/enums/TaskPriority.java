package timesheets.enums;

//This enum represents the different priorities that a task can have in the timesheet application.
public enum TaskPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
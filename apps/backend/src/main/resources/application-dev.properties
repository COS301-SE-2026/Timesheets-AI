# # spring.flyway.enabled=true
# # spring.flyway.locations=classpath:db/migration
# # spring.flyway.baseline-on-migrate=true

# # spring.datasource.url=${SPRING_DATASOURCE_URL}
# # spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# # spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# # spring.datasource.driver-class-name=org.postgresql.Driver

# # spring.jpa.hibernate.ddl-auto=validate
# # spring.jpa.show-sql=true

# # server.port=${SERVER_PORT:8080}

# # management.endpoints.web.exposure.include=health
# # management.endpoint.health.show-details=always

# # why is this false? that mean flyway is not active when testing locally 
# spring.flyway.enabled=true

# # FIX: this still say 'update' and not 'validate'
# # other problem is that properties files take higeher precedence than YAML files so application-dev.properties is overwritting the settings in application.yml 
# # FIX: environment variables are different 
# # spring.jpa.hibernate.ddl-auto=update #old one 
# spring.jpa.hibernate.ddl-auto=validate 


# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# spring.jpa.show-sql=true
# server.port=8080

# management.endpoints.web.exposure.include=health
# management.endpoint.health.show-details=always
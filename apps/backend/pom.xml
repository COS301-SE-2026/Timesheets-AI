<?xml version="1.0" encoding="UTF-8"?>
<!--
This file tells Maven (builds the project, manages dependencies and runs tests)
* who you are (groupId, artifactId, version)
* how to build 
* the dependencies, modules and packages used

Lifecycle of Maven:
compile -> test -> package -> install -> run

** For the test **
it must end with:
- *Test.java
- *Tests.java
- *TestCase.java
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    
    <modelVersion>4.0.0</modelVersion>

    <!-- Spring Boot Parent - provides dependency management -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <groupId>com.timesheetai</groupId>
    <artifactId>timesheet-ai</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>timesheet-ai</name>
    <description>Timesheets AI Backend with Spring Boot</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>

        <!-- SonarQube exclusion where we want it to ignore the migration files -->
        <!-- solution from: https://www.baeldung.com/sonar-exclude-violations-->
        <sonar.exclusions>**/db/migration/**/*.sql</sonar.exclusions>
    </properties>

    <dependencies>
        <!-- Spring Boot Web - REST APIs -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Data JPA - Database access -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Spring Boot Security - Authentication -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- JWT - For stateless authentication -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>

        <!-- PostgreSQL Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Validation - For request validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Testcontainers for integration tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- RestAssured for integration test HTTP assertions -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Boot Test - Unit and Integration tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Security Test - For testing security -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- SpringDoc for documentation  -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.5</version>
        </dependency>

        <!-- So flyway is not working right now, will keep this for future reference  -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
            <version>${flyway.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway.version}</version>
            <scope>runtime</scope>
        </dependency>
        
        <!-- Lombok reduces boilerplate code by auto-generating getters, setters,
             constructors and builders at compile time using annotations like
             @Data, @Builder, @NoArgsConstructor and @AllArgsConstructor.
             marked as optional so it is not included in the final JAR -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Data Redis provides RedisTemplate and other utilities for
             interacting with Redis. Used here for token blacklisting on logout -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!--using the most commonly used Java libraries for TOTP-based 2FA -->
        <!--code from a medium article-->
        <dependency>
            <groupId>com.warrenstrange</groupId>
            <artifactId>googleauth</artifactId>
            <version>1.5.0</version>
        </dependency>

        <!-- this is a google API client library that we will use for OAuth2 authentication and will use for SSO-->
        <!--code from maven central repository-->
        <dependency>
            <groupId>com.google.api-client</groupId>
            <artifactId>google-api-client</artifactId>
            <version>2.4.0</version>
        </dependency>

        <!-- still part of the API client but it this is the parser implementation-->
        <!--Gson dependancy from: https://developers.google.com/api-client-library/java/google-api-java-client/oauth2-->
        <dependency>
            <groupId>com.google.api-client</groupId>
            <artifactId>google-api-client-gson</artifactId>
            <version>2.4.0</version>
        </dependency>

        <!-- library to automatically load environment variable from the .env file to the application-->
        <!-- was having trouble with manually loading things-->
        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>4.0.0</version>
        </dependency>

    </dependencies>

   <build>
        <plugins>
            <!-- Maven Compiler Plugin - Lombok annotation processing -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Maven Surefire Plugin - Runs unit tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
                <configuration>
                    <environmentVariables>
                        <DOCKER_HOST>unix:///var/run/docker.sock</DOCKER_HOST>
                    </environmentVariables>
                </configuration>
            </plugin>

            <!-- JaCoco to see the coverage-->
            <!-- code from: https://medium.com/@truongbui95/jacoco-code-coverage-with-spring-boot-835af8debc68 -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.11</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            
            <!-- Spring Boot Maven Plugin - packages the app into a runnable JAR and provides
                the spring-boot:run command. Lombok is excluded from the final JAR because
                it is only needed at compile time, not at runtime -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!-- okay so below I am adding the things that are going to be related to backend code quality: code from Java Code Geeks-->
            <!-- this is the spotless is like Prettier in JS-->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.43.0</version>
                <configuration>
                    <java>
                        <!-- it is using the google Java format and it removes unused imports to remove clutter and have clean code-->
                        <googleJavaFormat/>
                        <removeUnusedImports/>
                        <!-- adding these so that there is consistency with how the files are cause I kept getting errors-->
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <!-- we want it to be at the very early stage of the build-->
                        <phase>validate</phase>
                        <goals>
                            <!-- we want it to simply check formatting, not auto-fix anything-->
                            <goal>check</goal> 
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- this is the checkstyle this is what will be used to help the code behave consistently: code from Geek for Geeks-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <configLocation>google_checks.xml</configLocation>
                    <encoding>UTF-8</encoding>
                    <consoleOutput>true</consoleOutput>

                    <!-- the build should fail if the checks don't pass-->
                    <failsOnError>true</failsOnError>

                    <!--the test directory gets included-->
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                </configuration>
            </plugin>

            <!-- this is the PMD to look for bad coding practices and possible bugs: code from PDM user docs-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.23.0</version>
                <configuration>
                    <consoleOutput>true</consoleOutput>      
                    <includeTestSourceDirectory>true</includeTestSourceDirectory> 
                    <failOnViolation>true</failOnViolation>
                </configuration>
            </plugin>

            <!-- this is the SpotBugs it will help us to identify code that can crash the system-->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.8.5.0</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>High</threshold>
                    <!--<threshold>Medium</threshold>-->
                    <failOnError>true</failOnError>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory> 
                </configuration>
            </plugin>
            <!-- this is the end of the code quality stuff I added-->

        </plugins>
    </build>
</project>
